{
  "name": "game-ts",
  "version": "1.0.0",
  "description": "game written in typescript",
  "license": "ISC",
  "author": "max paulus",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --https",
    "type-check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "typescript": "^5.3.3",
    "vite": "^7.1.11"
  }
}
